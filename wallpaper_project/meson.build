project('wallpaper_project', 'cpp',
  version : '0.1',
  license: 'MIT',
  default_options : ['warning_level=3', 'cpp_std=c++14'])

sqlite_dep = dependency('SQLite3')

inc = include_directories('include')

subdir('include')
subdir('src')
